// import { Component } from '@angular/core';
// import { UserService } from '../user.service';

// @Component({
//   selector: 'app-login',
//   templateUrl: './login.component.html',
//   styleUrls: ['./login.component.css']
// })
// export class LoginComponent {
//   email!: string;
//   password!: string;

//   constructor(private userService: UserService) {}

//   login(): void {
//     // Выполните здесь код для отправки запроса на сервер для авторизации
//     // Используйте this.email и this.password для получения значений из полей формы

//     // Примерный код для авторизации с использованием UserService
//     if (this.userService.login(this.email, this.password)) {
//       // Авторизация успешна, выполните необходимые действия (например, перенаправление на главную страницу)
//     } else {
//       // Обработка ошибки авторизации
//     }
//   }
// }
